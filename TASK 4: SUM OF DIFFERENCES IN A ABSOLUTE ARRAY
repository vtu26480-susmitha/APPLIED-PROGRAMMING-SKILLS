PROGRAM:
class Solution {
    public int[] getSumAbsoluteDifferences(int[] nums) {
        int n = nums.length;
        int[] result = new int[n];
        long[] prefix = new long[n];  // use long to avoid overflow

        // Build prefix sum
        prefix[0] = nums[0];
        for (int i = 1; i < n; i++) {
            prefix[i] = prefix[i - 1] + nums[i];
        }

        for (int i = 0; i < n; i++) {
            long leftSum = i > 0 ? i * (long)nums[i] - prefix[i - 1] : 0;
            long rightSum = i < n - 1 ? (prefix[n - 1] - prefix[i]) - (n - i - 1) * (long)nums[i] : 0;
            result[i] = (int)(leftSum + rightSum);
        }

        return result;
    }
}

OUPUT:
Input
nums =
[2,3,5]
Output
[4,3,5]
